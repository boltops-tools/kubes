apiVersion: v1
kind: Service
metadata:
  name: <%= app %>
  labels:
    app: <%= app %>
    role: web
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: <%= app %>
    role: web
  type: ClusterIP
